# Bun migration

This project is **Bun-only**. Runtime, install, and production builds all use Bun 1.4.0.

See `BUN.md` for current commands and how the pin is enforced.

Do not introduce `package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, or Node-based CI/build commands.
