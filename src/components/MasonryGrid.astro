---
/**
 * MasonryGrid Component
 * Re-designed as a balanced, gapless, infinitely scrolling creative showcase.
 * Features 3 parallel vertical scrolling columns (up/down/up) that loop seamlessly.
 * Respects prefers-reduced-motion.
 */

const items = [
  { src: "/assets/Turismo.mp4", title: "Travel & Tourism", ratio: 1.77 }, 
  { src: "/assets/Tiene_3.mp4", title: "Social Content", ratio: 1.77 },
  { src: "/assets/claro_catalogo.mp4", title: "E-commerce Catalog", ratio: 1.77 },
  { src: "/assets/consorcio_9-16.mp4", title: "Real Estate", ratio: 0.56 }, 
  { src: "/assets/dic-2-tomas-del-castillo-story_027ad7d.mp4", title: "Influencer Content", ratio: 0.56 },
  { src: "/assets/VENTA_PRODUCTO_1-1.mp4", title: "Product Showcase", ratio: 1 },
  { src: "/assets/Real Estate.mp4", title: "Property Tour", ratio: 0.56 },
  { src: "/assets/SPEAKER_9-16.mp4", title: "Event Speaker", ratio: 0.56 },
  { src: "/assets/Kamay_Render_Vertical.mp4", title: "3D Rendering", ratio: 0.56 },
  { src: "/assets/Catalogo_Heineken.mp4", title: "Brand Catalog", ratio: 1.77 },
  { src: "/assets/Mercado_Libre.mp4", title: "Retail Ads", ratio: 1 },
  { src: "/assets/Tiene_4.mp4", title: "Fashion Creative", ratio: 1.77 },
  { src: "/assets/BODEGA_GOL_PROMO_9-16.mp4", title: "Sports Promotion", ratio: 0.56 },
  { src: "/assets/Tiene_2.mp4", title: "Lifestyle", ratio: 1 },
  { src: "/assets/Deportes_resultados.mp4", title: "Sports Results", ratio: 1 },
];

// Balanced distribution across 5 columns
const col1 = [items[0], items[3], items[5]];
const col2 = [items[1], items[4], items[10]];
const col3 = [items[2], items[6], items[13]];
const col4 = [items[9], items[7], items[14]];
const col5 = [items[11], items[8], items[12]];

// Duplicate lists for seamless looping
const col1Double = [...col1, ...col1];
const col2Double = [...col2, ...col2];
const col3Double = [...col3, ...col3];
const col4Double = [...col4, ...col4];
const col5Double = [...col5, ...col5];

const getAspectClass = (ratio: number) => {
  if (ratio === 0.56) return "aspect-[9/16]";
  if (ratio === 1) return "aspect-square";
  return "aspect-video";
};
---

<section id="showcase" class="py-16 md:py-24 bg-background relative overflow-hidden z-10 reveal">
  <div class="w-full max-w-none px-4 md:px-8 relative">
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4 md:gap-6 lg:gap-8 h-[80vh] min-h-[500px] max-h-[1000px] overflow-hidden group/grid relative border-y border-border/10 py-4">
      
      {/* Column 1 (Scrolls Up) */}
      <div class="flex flex-col gap-4 md:gap-6 lg:gap-8 scroll-container">
        <div class="flex flex-col gap-4 md:gap-6 lg:gap-8 animate-scroll-up">
          {col1Double.map((item) => (
            <div 
              class={`w-full ${getAspectClass(item.ratio)} rounded-lg overflow-hidden bg-card border border-border/20 shadow-sm relative group/item hover:scale-[1.01] hover:border-primary/30 transition-all duration-500`}
            >
              <video 
                data-src={item.src} 
                muted 
                playsinline 
                loop 
                preload="none" 
                class="w-full h-full object-cover block lazy-video"
              ></video>
            </div>
          ))}
        </div>
      </div>

      {/* Column 2 (Scrolls Down) */}
      <div class="flex flex-col gap-4 md:gap-6 lg:gap-8 scroll-container">
        <div class="flex flex-col gap-4 md:gap-6 lg:gap-8 animate-scroll-down">
          {col2Double.map((item) => (
            <div 
              class={`w-full ${getAspectClass(item.ratio)} rounded-lg overflow-hidden bg-card border border-border/20 shadow-sm relative group/item hover:scale-[1.01] hover:border-primary/30 transition-all duration-500`}
            >
              <video 
                data-src={item.src} 
                muted 
                playsinline 
                loop 
                preload="none" 
                class="w-full h-full object-cover block lazy-video"
              ></video>
            </div>
          ))}
        </div>
      </div>

      {/* Column 3 (Scrolls Up - Hidden on Mobile, visible from sm) */}
      <div class="hidden sm:flex flex-col gap-4 md:gap-6 lg:gap-8 scroll-container">
        <div class="flex flex-col gap-4 md:gap-6 lg:gap-8 animate-scroll-up">
          {col3Double.map((item) => (
            <div 
              class={`w-full ${getAspectClass(item.ratio)} rounded-lg overflow-hidden bg-card border border-border/20 shadow-sm relative group/item hover:scale-[1.01] hover:border-primary/30 transition-all duration-500`}
            >
              <video 
                data-src={item.src} 
                muted 
                playsinline 
                loop 
                preload="none" 
                class="w-full h-full object-cover block lazy-video"
              ></video>
            </div>
          ))}
        </div>
      </div>

      {/* Column 4 (Scrolls Down - Hidden, visible from md) */}
      <div class="hidden md:flex flex-col gap-4 md:gap-6 lg:gap-8 scroll-container">
        <div class="flex flex-col gap-4 md:gap-6 lg:gap-8 animate-scroll-down">
          {col4Double.map((item) => (
            <div 
              class={`w-full ${getAspectClass(item.ratio)} rounded-lg overflow-hidden bg-card border border-border/20 shadow-sm relative group/item hover:scale-[1.01] hover:border-primary/30 transition-all duration-500`}
            >
              <video 
                data-src={item.src} 
                muted 
                playsinline 
                loop 
                preload="none" 
                class="w-full h-full object-cover block lazy-video"
              ></video>
            </div>
          ))}
        </div>
      </div>

      {/* Column 5 (Scrolls Up - Hidden, visible from lg) */}
      <div class="hidden lg:flex flex-col gap-4 md:gap-6 lg:gap-8 scroll-container">
        <div class="flex flex-col gap-4 md:gap-6 lg:gap-8 animate-scroll-up">
          {col5Double.map((item) => (
            <div 
              class={`w-full ${getAspectClass(item.ratio)} rounded-lg overflow-hidden bg-card border border-border/20 shadow-sm relative group/item hover:scale-[1.01] hover:border-primary/30 transition-all duration-500`}
            >
              <video 
                data-src={item.src} 
                muted 
                playsinline 
                loop 
                preload="none" 
                class="w-full h-full object-cover block lazy-video"
              ></video>
            </div>
          ))}
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  .scroll-container {
    height: 100%;
    overflow: hidden;
  }

  @keyframes scroll-up {
    0% { transform: translateY(0); }
    100% { transform: translateY(-50%); }
  }

  @keyframes scroll-down {
    0% { transform: translateY(-50%); }
    100% { transform: translateY(0); }
  }

  .animate-scroll-up {
    animation: scroll-up 45s linear infinite;
  }

  .animate-scroll-down {
    animation: scroll-down 45s linear infinite;
  }



  /* Accessibility and reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .animate-scroll-up,
    .animate-scroll-down {
      animation: none;
      transform: none;
    }
  }
</style>

<script>
  const initLazyVideos = () => {
    const videoObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        const video = entry.target as HTMLVideoElement;
        if (entry.isIntersecting) {
          if (video.dataset.src) {
            video.src = video.dataset.src;
            video.load();
            video.removeAttribute('data-src');
          }
          video.play().catch(() => {});
        } else {
          video.pause();
        }
      });
    }, { rootMargin: "150px", threshold: 0.1 });

    document.querySelectorAll("video.lazy-video").forEach(v => videoObserver.observe(v));
  };

  initLazyVideos();

  document.addEventListener('astro:page-load', () => {
    initLazyVideos();
  });
</script>
